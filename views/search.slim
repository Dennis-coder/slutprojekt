div 
    div class='search_bar'
        form class='space' action='/home/search' method='post'
            input type="text" placeholder='Search...' name='search_term'
            button type="submit"
                img src="/media/search.svg"
    -if results == nil
        p No results
    -else
        -for result in results
            div class='space user'
                h1= "#{result.username}"
                -status = Friend.status?(result.id, @user.id)
                -if status == "friends"
                    form action='/home/search/#{result.id}/delete' method='post'
                        button type='submit' Already friends
                -elsif status == "pending"
                    -if Friend.sender(@user.id, result.id) == @user.id
                        form action='/home/search/#{result.id}/delete' method='post'
                            button type='submit' Pending request
                    -else
                        form action='/home/#{result.id}/accept' method='post'
                            button type='submit' Add friend
                -else
                    form action='/home/#{result.id}/add' method='post'
                        button type='submit' Send friend request